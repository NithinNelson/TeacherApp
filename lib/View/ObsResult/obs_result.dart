
import 'package:flutter/material.dart';

class ObsResult extends StatelessWidget {
  const ObsResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "OBS Result",
      ),
    );
  }
}
